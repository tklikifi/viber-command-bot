#!/usr/bin/env bash

DIR="$(dirname $0)/.."

if [ ! -f "$DIR/.virtualenvrc" ]; then
    echo "ERROR: Virtual environment has not been created."
    exit 1
fi

source "$DIR/.virtualenvrc"

"$DIR/vibercommandbot/utils/send_message.py" "$@"
